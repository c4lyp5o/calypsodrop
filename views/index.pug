extends layout

block content
  if user != 'Public'
    h1 Hello #{user}
  else
    h1 Hello anon!
   
  form(action='/' enctype='multipart/form-data' method='POST') 
    legend Upload anything you want here
    br
    label(for='thefile') Select a file:
    input#drops(type='file' name='drops')
    br
    br
    input(type="submit", value="Submit", class="btn btn-primary")
